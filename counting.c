#include <stdio.h>
int main(){
int d;
 printf("Enter a integer: ");

    scanf("%d", &d);

    if ((d >= 1 && d <= d) || (d >= 1 && d<= d))

        printf("%d is an counting", d);

    else

        printf("%d is not an counting", d);


    return 0;
}


	